resources:
  volumes:
    landing_volume:
      catalog_name: ${var.catalog}
      schema_name: ${resources.schemas.bronze_schema.name}
      name: landing
      comment: "Landing volume for ecommerce data"