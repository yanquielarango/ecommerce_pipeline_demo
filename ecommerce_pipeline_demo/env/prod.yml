targets:
  prod:
    mode: production

    workspace:
      host: https://<prod-workspace>.azuredatabricks.net

      root_path: /Workspace/.bundle/${bundle.name}/${bundle.target}

    variables:
      catalog: dbr_dev