targets:
  dev:
    default: true

    workspace:
      host: https://adb-7405606278809955.15.azuredatabricks.net

    variables:
      catalog: dbr_dev
      sql_warehouse_id: "9d0ad0844cc205a3"
      bronze_managed_storage: "abfss://unity-catalog@dlspldatabricks.dfs.core.windows.net/managed/dbr_dev/ecommerce_bronze"

    resources:
      volumes:
        landing_volume:
          name: landing
          catalog_name: ${var.catalog}
          schema_name: ${resources.schemas.bronze.name}
          volume_type: MANAGED